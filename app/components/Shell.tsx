'use client';
 'use client'; import Link from 'next/link'; import {useState} from 'react'; import {activeAddress,connectInjectedWallet} from '../../lib/genlayer';
export default function Shell({children}:{children:React.ReactNode}){const [wallet,setWallet]=useState(activeAddress());async function connect(){const a=await connectInjectedWallet();setWallet(a||activeAddress())}return <><header className="appbar"><Link href="/" className="logo"><span>✦</span> AFTERCARE</Link><nav><Link href="/rounds">Funding rounds</Link><Link href="/studio">Submit a claim</Link><Link href="/claims">My claims</Link></nav><button className="connect" onClick={connect}>{wallet?`${wallet.slice(0,6)}…${wallet.slice(-4)}`:'Connect wallet'}</button></header><div className="networkbar"><span className="dot green"/> StudioNet <span className="muted">·</span> contract connected <span className="contract">0x5FF0…D433</span></div>{children}</>}
